import SwiftUI

struct MapScreen: View {
    var body: some View {
        VStack {
            Text("Account Screen")
        }
        .navigationTitle("Account")
    }
}

#Preview {
    MapScreen()
}
